{% extends 'base.html.twig' %}

{% block title %}Jobs{% endblock %}

{% block body %}
    <h1>Jobs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job.id }}</td>
            </tr>
            <tr>
                <th>JobDescription</th>
                <td>{{ job.jobDescription }}</td>
            </tr>
            <tr>
                <th>JobNom</th>
                <td>{{ job.jobNom }}</td>
            </tr>
            <tr>
                <th>JobEntreprise</th>
                <td>{{ job.jobEntreprise }}</td>
            </tr>
            <tr>
                <th>JobLieu</th>
                <td>{{ job.jobLieu }}</td>
            </tr>
            <tr>
                <th>JobShort</th>
                <td>{{ job.jobShort }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_jobs_index') }}">back to list</a>

    <a href="{{ path('app_jobs_edit', {'id': job.id}) }}">edit</a>

    {{ include('jobs/_delete_form.html.twig') }}
{% endblock %}
